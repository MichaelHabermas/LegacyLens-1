# LegacyLens dev dependencies (also in pyproject.toml [project.optional-dependencies].dev)
# Install with: pip install -e ".[dev]"  or  pip install -r requirements-dev.txt
-r requirements.txt
black>=24.0.0
ruff>=0.1.0
mypy>=1.0.0
